# Sprint 01 · Initialisation

## Block

BLOCK 1 · Setup (MVP)

## Goal

Create the project skeleton, unified development environment, and architectural foundations.

## Requirements

* DevContainer environment configured with Docker.
* Standard project structure (`src/`, `tests/`, etc.).
* Python dependency management defined (`pyproject.toml`).
* GIS C-libraries properly integrated inside the container environment (GDAL, PDAL, Rasterio).

## Tasks

- [ ] Define the base `pyproject.toml` specification.
- [ ] Create `Dockerfile` / `docker-compose.yml` for DevContainer ensuring GDAL and PDAL are installed at system level.
- [ ] Create directory structure (`src/` and `tests/`).
- [ ] Verify IDE and Claude Code interaction inside this environment.

## Deliverable

Structured local repository and a functional Docker container serving as the unified development environment.

## Notes

<!-- Any relevant observations, issues encountered, or deviations from the plan -->

## Documents Updated

- [docs/current_state.md](../current_state.md)
- [docs/next_tasks.md](../next_tasks.md)

## Date Started

2026-09-04

## Date Completed

<!-- YYYY-MM-DD -->
